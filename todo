- Check github action workflows
- add PROJECTNUMBER support in makeDoxyfileForLibrary

- for now we need to append this to the SConstruct and inside each glibrary SConstruct
env.Append(CXXFLAGS=['-std=c++17'])


Add this at the top of each class definition
// Class GMAPCOORDINATES
// Thread Type: GLOBAL
