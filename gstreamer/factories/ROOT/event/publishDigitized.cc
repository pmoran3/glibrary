// gstreamer
#include "gstreamerROOTFactory.h"

bool GstreamerRootFactory::publishEventDigitizedData(string detectorName, const vector<GDigitizedData*>* digitizedData) {

	if(rootfile == nullptr) return false;

	return true;
}
